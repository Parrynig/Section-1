// 1. Create an array of names and add all names longer than 5 characters in a new array.
// 2. Create a function that takes a range and returns all prime numbers in that range.
// 3. Create a function that takes a range and returns sum of all numbers.
// 4. Remove all odd numbers from an array of number.

// const factorial = (n) => {
//     let f=1;
//     for(let i=2; i<=n;i++){
//         f*=i;
//     }
//     return f;
// }

const getAvg = function (m1,m2,m3) {
    const avg =(m1+m2+m3)/3;
    // console.log(avg);
    return avg;
    
}

let arr = [dhsfh, kdashf,jsadfh, jfh, hskd, hdfjd, ksdjfg, kjfwgh];

const longarr = function (a ) {
    for(let i= 0; i<a.lenght; i++){
        
    }
    
    


    // console.log(avg);
    return avg;
    
}